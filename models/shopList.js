class shopList{
    constructor (title, creatorId, members, products, summary){
        this.title = title;
        this.creatorId = creatorId;
        this.members = members;
        this.products = products;
        this.summary = summary;
    }
}

export default shopList;